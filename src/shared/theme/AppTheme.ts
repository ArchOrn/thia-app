import { DefaultTheme } from '@react-navigation/native';

const AppTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#1bb4da',
    dark: '#080e32',
  },
};

export default AppTheme;
